% Atoms are like immutable literals with their own names as values. They begin
% with lowercase characters:

atom. % atom

atoms_rule. % atoms_rule

% Though they can be cheated:

'Atoms can be cheated'. % 'Atoms can be cheated'

atom = 'atom'. % atom
